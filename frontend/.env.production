# Used by `npm run build` (production mode). Not a secret — this is the
# publicly reachable backend URL, the same one a browser would call anyway.
VITE_API_URL=https://azure-transcript-analysis.onrender.com
